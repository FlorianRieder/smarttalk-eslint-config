// Every finding in this file is intentional; check.mjs expects exactly these.
export function gruss(_ignoriert: string, name: string): string {
  const unbenutzt = 1;
  const wert: any = name;
  var alt = wert;
  return alt;
}
